package domain

// Package domain is intentionally small in the first skill-registry slice.
// The service owns catalog validation in types and keeps execution concerns out
// of the registry boundary.
