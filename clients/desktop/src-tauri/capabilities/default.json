{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-shell-metadata",
  "description": "Allow the main desktop shell window to use only audited Tauri core defaults, runtime metadata and the fixed NexusIM local-store key-value commands.",
  "windows": ["main"],
  "permissions": ["core:default", "allow-runtime-metadata", "allow-local-store"]
}
