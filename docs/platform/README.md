# NexusIM Platform Documents

This directory owns cross-cutting platform capability documents that are not
service-specific SDDs.

| Document | Scope |
| --- | --- |
| `middleware-catalog.md` | Middleware capability taxonomy, adoption rules and runtime profile registry. |

Keep platform docs focused on shared capabilities. Service internals still live
under `docs/sdd/` and `docs/runbook/service-briefs/`.
